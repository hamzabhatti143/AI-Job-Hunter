# Virtual environment — largest folder, must never be uploaded
.venv/
venv/
env/

# Python cache
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.pyc
*.pycache

# Environment variables (secrets)
.env
.env.*

# Uploaded resumes — deleted after processing but exclude anyway
uploads/

# Build / distribution
dist/
build/
*.egg-info/
*.egg
.eggs/

# Database / migrations cache
*.db
*.sqlite3

# Alembic env (auto-generated, not needed in repo)
# migrations/ — keep this so schema migrations are tracked

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.iml
